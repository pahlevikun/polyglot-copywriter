---
description: Rewrite a draft with polyglot-copywriter
argument-hint: [file path, or paste the draft]
---
Use the `polyglot-copywriter` skill in rewrite mode on: $ARGUMENTS

Resolve language, register, overlay, and use case first. Load
`references/substance.md`. Diagnose missing truth, substance, development,
sentences, and craft in that order before editing. Preserve protected artifacts
(numbers, names, links, commands). Mark missing facts with `[TK: …]` instead of
inventing detail. If I have given you a sample of my own writing anywhere in
this conversation, match it ahead of the skill's defaults without importing
facts from it.
