# {usecase_id} — use case pack

**Status:** `{status}`

Use cases are orthogonal to language. This file teaches **structure**, not vocabulary.

## When to load

Load when Step 1 matches `{usecase_id}` or an alias in [registry.json](../../registry.json).

## Sentence 1 job

State what the reader should know or do.

## Section order

1.
2.
3.

## Default register

`{default_register}` unless the user overrides.

## Do not steal from other formats

- An ad is not an RFC.
- Chat is not a letter.
- This pack does not change protected code or identifiers that are the task target.
