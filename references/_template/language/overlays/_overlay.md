# {overlay_id} overlay — {language_id}

**Type:** `{overlay_type}` (`regional` | `slang` | `mix`)
**Status:** `{status}`

## When to load

Load **only** when `regional_voice: {overlay_id}` (or alias) is active for `{language_id}`. One overlay at a time unless the user asked for a named mix.

## Particle and slang jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| | | `tipis` / `sedang` / `kental` |

## Caps

- No phonetic parody or accent spelling.
- No marker soup. Skip a form if you cannot state its job.
- `kental` only with explicit user request and enough examples; otherwise cap at `sedang`.

## Forbidden caricature

- List expired or stereotype forms to avoid.
