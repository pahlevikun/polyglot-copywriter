# vi-north — Northern Vietnamese (Hanoi)

**Type:** `regional` | **Status:** `validated`

## When to load

`language: vietnamese` + `regional_voice: vi-north`. Northern baseline — national standard lean.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `không` | Negation | `tipis+` |
| `nên` | Should/therefore next step | `tipis+` |
| `đang` | Progressive state | `tipis+` |
| `ạ` | Polite sentence tail (formal) | `tipis+` |

## Examples (bug explanation)

- `.env` không có `API_TOKEN`, nên `loadConfig` trả về `undefined`. Chạy `npm test -- config` nhé.

## Caps

- Default northern/standard lean for work.

## What NOT to mix

- Southern particles in northern output.

## Forbidden caricature

- Southern particles in northern output.
