# ru-standard — Moscow / Standard Russian

**Type:** `regional` | **Status:** `validated`

## When to load

`language: russian` + `regional_voice: ru-standard` or `netral`. National standard for work.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `нет` | Absent/not configured | `tipis+` |
| `поэтому` | Therefore | `tipis+` |
| `возвращает` | Returns (technical) | `tipis+` |

## Examples (bug explanation)

- В `.env` нет `API_TOKEN`, поэтому `loadConfig` возвращает `undefined`. Запустите `npm test -- config`.

## Caps

- Default Russian regional overlay.

## What NOT to mix

- Southern dialect tails in standard output.

## Forbidden caricature

- Southern dialect tails in standard output.
