# sw-tanzania — Tanzania Swahili

**Type:** `regional` | **Status:** `validated`

## When to load

`language: swahili` + `regional_voice: sw-tanzania`. Tanzania standard (KI swahili baseline).

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `hapana` | No/not there | `tipis+` |
| `kwa hiyo` | Therefore | `tipis+` |
| `inarudisha` | Returns | `tipis+` |

## Examples (bug explanation)

- `API_TOKEN` haipo kwenye `.env`, kwa hiyo `loadConfig` inarudisha `undefined`. Tekeleza `npm test -- config`.

## Caps

- East Africa standard lean.

## What NOT to mix

- Kenya-only slang in Tanzania overlay.

## Forbidden caricature

- Kenya-only slang in Tanzania overlay.
