# arz-cairo — Cairo Egyptian Arabic

**Type:** `regional` | **Status:** `validated`

## When to load

`language: arz` + `regional_voice: arz-cairo`. Cairo urban Egyptian — default for arz.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `مش` | Negation | `tipis+` |
| `يعني` | So/meaning | `tipis+` |
| `لازم` | Need to | `tipis+` |
| `جرّب` | Try (imperative) | `tipis+` |

## Examples (bug explanation)

- `API_TOKEN` مش موجود في `.env`، يعني `loadConfig` بيرجع `undefined`. لازم تجرب `npm test -- config`.

## Caps

- arz is Egyptian — do not mix MSA-only chains without reason.

## What NOT to mix

- Fusha formal every sentence unless register is baku.

## Forbidden caricature

- Fusha formal every sentence unless register is baku.
