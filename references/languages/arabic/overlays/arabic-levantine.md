# arabic-levantine — Levantine Arabic (umbrella)

**Type:** `regional` | **Status:** `validated`

## When to load

`language: arabic` + `regional_voice: arabic-levantine`. Levant-informed — umbrella honesty applies.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `ما … مش` | Double negation pattern (light) | `tipis+` |
| `هلق` | Now/then connector (Levant spoken) | `tipis` |
| `يعني` | Explains implication | `tipis+` |

## Examples (bug explanation)

- `API_TOKEN` مش موجود بـ`.env`، يعني `loadConfig` عم يرجّع `undefined`.

## Caps

- Thin overlay under umbrella `arabic`; ask for user example for `kental`.

## What NOT to mix

- Mixing Egyptian and Levant tails in one sentence.

## Forbidden caricature

- Mixing Egyptian and Levant tails in one sentence.
