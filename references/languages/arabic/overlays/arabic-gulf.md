# arabic-gulf — Gulf Arabic (umbrella)

**Type:** `regional` | **Status:** `validated`

## When to load

`language: arabic` + `regional_voice: arabic-gulf`. Gulf-informed — umbrella honesty applies.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `ما في` | There is not (Gulf spoken pattern) | `tipis+` |
| `بعد` | Still/yet in diagnostic | `tipis+` |
| `لازم` | Must/need to — next step | `tipis+` |

## Examples (bug explanation)

- ما في `API_TOKEN` بـ`.env`، لازم تشغّل `npm test -- config`.

## Caps

- Umbrella honesty — offer MSA-neutral or user example if forms thin.

## What NOT to mix

- Levant/Egyptian markers in Gulf-labelled output.

## Forbidden caricature

- Levant/Egyptian markers in Gulf-labelled output.
