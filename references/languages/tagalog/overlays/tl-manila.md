# tl-manila — Manila Tagalog

**Type:** `regional` | **Status:** `validated`

## When to load

`language: tagalog` + `regional_voice: tl-manila`. Metro Manila Filipino baseline.

## Particle and grammar jobs

| Form | Pragmatic job | Intensity |
|---|---|---|
| `wala` | Absent | `tipis+` |
| `kaya` | Therefore/so | `tipis+` |
| `nagbabalik` | Returns | `tipis+` |
| `po` | Polite tail (work) | `tipis+` |

## Examples (bug explanation)

- Walang `API_TOKEN` sa `.env`, kaya nagbabalik ng `undefined` ang `loadConfig`. Patakbuhin ang `npm test -- config` po.

## Caps

- Taglish is separate mix overlay.

## What NOT to mix

- English code-switch in every clause unless taglish.

## Forbidden caricature

- English code-switch in every clause unless taglish.
